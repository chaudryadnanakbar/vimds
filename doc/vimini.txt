*vimini.txt*    For My Awesome Vim Deepseek Plugin    Last change: 2026 Aug 29

==============================================================================
CONTENTS                                                      *vimini*

  1. Introduction.........................................|vimini-introduction|
  2. Usage................................................|vimini-usage|
  3. Commands.............................................|vimini-commands|
  4. Configuration........................................|vimini-configuration|

==============================================================================
INTRODUCTION                                          *vimini-introduction*

This was a Vim plugin to use Gemini via APIs written in Python 3. I plan to make
it for a deepseek api with C based agent , rest of the protocols and functions will
be kep intact. Please check the main Architecture Doc . Will keep it intact onyl change 
what is needed.

First Phase make a simulated one then replace with configrable one to allow connection 
to any of AI models. 

To Keep it simple the UI layouts and functions will remain same as in the original one 

Step 1: Copy the vim plugin vimini.vim t vimdeepseek.vim and duplicate the plugin 

Step 2. Connect all the functions to vim and run in simulation mode

Step 3. Replace the agent written in python3 with C based actual service 

Step 4. Debug and test and make it awesome




==============================================================================
USAGE                                                         *vimini-usage*

Install the plugin using your favorite plugin manager.

==============================================================================
COMMANDS                                                    *vimini-commands*

*:ViminiChat*
    Opens the interactive chat window to communicate with Gemini.

*:ViminiCommit* [-n] [-r] [-a] [instruction]
    Generates a commit message and commits changes using Gemini.
    -n: Omit co-author trailer.
    -r: Regenerate commit message for HEAD and amend.
    -a: Amend working tree changes into HEAD and regenerate commit message.
    [instruction]: Optional hint for the commit message generation.

==============================================================================
CONFIGURATION                                           *vimini-configuration*

g:vimini_user_name
    Set this variable in your |vimrc| to customize the greeting message.
    Example: >
        let g:vimini_user_name = 'Alice'
    <
    Default: 'Vim User'

==============================================================================
